import { HoverHiglightDirective } from './hover-higlight.directive';

describe('HoverHiglightDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverHiglightDirective();
    expect(directive).toBeTruthy();
  });
});
